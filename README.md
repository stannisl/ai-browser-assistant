# AI Browser Assistant

Автономный AI-агент для управления веб-браузером. Выполняет сложные многошаговые задачи без предопределённых сценариев — агент сам анализирует страницы и принимает решения.

## 🎯 Что умеет

- **Автономная навигация** — открывает сайты, находит нужные элементы, заполняет формы
- **Поиск информации** — ищет вакансии, товары, данные на любых сайтах
- **Работа с формами** — авторизация, регистрация, оформление заказов
- **Адаптивность** — восстанавливается после ошибок, обрабатывает попапы и модальные окна
- **Безопасность** — запрашивает подтверждение перед опасными действиями (оплата, удаление)

## 🚀 Быстрый старт

### Требования

- Go 1.24+
- Google Chrome / Chromium
- API ключ для LLM (OpenAI-совместимый API)

### Установка

```bash
# Клонирование репозитория
git clone https://github.com/stannisl/ai-browser-assistant.git
cd ai-browser-assistant

# Установка зависимостей
go mod tidy

# Сборка
go build -o ./bin/agent ./cmd/agent
```

### Запуск

Минимальный запуск
```bash
export ZAI_API_KEY="your-api-key"
./bin/agent
```

Рекомендованный
```bash
make build && ZAI_API_KEY=<key> ZAI_BASE_URL=https://api.z.ai/api/paas/v4/ ZAI_MODEL=glm-4.7-flash ./bin/agent
```

### Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `ZAI_API_KEY` | API ключ для LLM | — (обязательно) |
| `ZAI_BASE_URL` | URL API | `https://api.z.ai/v1` |
| `ZAI_MODEL` | Модель | `glm-4.5-flash` |
| `USER_DATA_DIR` | Директория сессии браузера | `./user-data` |
| `DEBUG` | Режим отладки | `false` |

## Структура проекта

```
ai-browser-assistant/
├── cmd/
│   └── agent/
│       └── main.go          # Точка входа, REPL
├── internal/
│   ├── agent/
│   │   ├── agent.go         # Основной цикл агента
│   │   └── executor.go      # Выполнение инструментов
│   ├── browser/
│   │   └── browser.go       # Управление браузером (go-rod)
│   ├── extractor/
│   │   └── extractor.go     # Извлечение элементов страницы
│   ├── llm/
│   │   ├── client.go        # Клиент LLM API
│   │   ├── prompts.go       # Системный промпт
│   │   └── tools.go         # Определения инструментов
│   ├── logger/
│   │   └── logger.go        # Логирование
│   └── types/
│       ├── agent.go         # Типы агента
│       ├── browser.go       # Типы браузера
│       └── errors.go        # Ошибки
├── configs/
│   └── config.yaml          # Конфигурация (опционально)
├── go.mod
├── go.sum
└── README.md
```

## 🔧 Инструменты агента

| Инструмент | Описание |
|------------|----------|
| `extract_page` | Получить список интерактивных элементов страницы |
| `navigate` | Перейти по URL |
| `click` | Кликнуть на элемент по ID |
| `type_text` | Ввести текст в поле по ID |
| `scroll` | Прокрутить страницу вверх/вниз |
| `press_key` | Нажать клавишу (Enter, Escape, Tab, стрелки) |
| `wait` | Подождать 1-10 секунд |
| `ask_user` | Задать вопрос пользователю |
| `confirm_action` | Запросить подтверждение опасного действия |
| `report` | Завершить задачу с отчётом |

## 🔒 Безопасность

Агент запрашивает подтверждение перед:
- Оплатой заказов
- Удалением данных
- Отправкой сообщений
- Любыми необратимыми действиями

```
🔒 CONFIRMATION REQUIRED
Action: Оплатить заказ на сумму 15000₽
Proceed? (yes/no): 
```

### Совместимые LLM

Любой OpenAI-совместимый API с поддержкой tool calling
